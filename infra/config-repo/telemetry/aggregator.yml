aggregator:
  kafka:
    bootstrap-servers: localhost:9092
    consumer-group-id: telemetry.aggregator
    sensor-topic: telemetry.sensors.v1
    snapshot-topic: telemetry.snapshots.v1
    consumer:
      poll-timeout: 100ms

spring:
  main:
    web-application-type: none
